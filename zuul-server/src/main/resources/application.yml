spring:
  application:
    name: zuul-server
server:
  port: 8054
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8501/eureka/
zuul:
  routes:
    test:
      path: /test/**
      url: http://www.google.com
