server:
  port: 8502
eureka:
  client:
    serviceUrl:
      defaultZone: http://test.tembin.com:8501/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/liusy456/config-server
          username: liusy456
          password: lsy417937