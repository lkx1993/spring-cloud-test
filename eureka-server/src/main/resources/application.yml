server:
  port: 8501

spring:
  application:
    name: eureka

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8501/eureka/
    register-with-eureka: false
    fetch-registry: false
